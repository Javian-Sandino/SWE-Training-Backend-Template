# Example .env for backend
# Copy this to .env and fill in your real credentials. Do NOT commit .env to git.

# For MongoDB Atlas (replace <user>, <password>, <cluster> and <dbname>):
# MONGODB_URI="mongodb+srv://<user>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority"

# For a local MongoDB server (default port 27017):
# MONGODB_URI="mongodb://localhost:27017/mydb"

# Optionally change the backend port (default 4000):
# PORT=4000
